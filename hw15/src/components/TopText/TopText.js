import React from "react";
import "./toptext.css";

const TopText = props => (
    <div className="textybox">
        <h1>Collect Fall Leaves!</h1> 
        <p>Click on the leaves to rake them up!  But trees get paranoid, so don't click the same leaf twice!</p>
    </div>
);

export default TopText;